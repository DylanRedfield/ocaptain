policies:
  - name: "KerasPolicy"
    epochs: 100
    max_history: 5
  - name: "MemoizationPolicy"
    max_history: 5
  - name: "FallbackPolicy"
    fallback_action_name: "action_default_fallback"
    nlu_threshold: 0.1
    core_threshold: 0.1
